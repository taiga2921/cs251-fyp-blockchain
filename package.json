{
  "name": "blockchain-ethereum-v1",
  "version": "1.0.0",
  "private": true,
  "description": "FYP surveillance platform — Ethereum EvidenceStore contract (Hardhat)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:ganache": "hardhat run scripts/deploy-ganache.js --network ganache",
    "verify:ganache": "hardhat run scripts/verify-deployment.js --network ganache",
    "export:abi": "hardhat run scripts/export-abi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18"
  }
}
